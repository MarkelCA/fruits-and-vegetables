services:
  app:
    build: .
    working_dir: /app
    container_name: fruits-and-vegetables
    command: php -S 0.0.0.0:8080 -t /app/public
    ports:
      - "8080:8080"
    volumes:
      - ..:/app
